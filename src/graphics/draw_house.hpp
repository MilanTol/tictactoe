#pragma once

#include <SFML/Graphics.hpp>

#include "../config.hpp"

void draw_house(sf::RenderWindow& window, color, sf::Vector2f position);
void draw_house(sf::RenderWindow& window, color, int column, int row);
